# Funzioni Cron

Questa directory contiene le funzioni Cloud schedulate (cron jobs) per operazioni automatiche.

## Note

Le vecchie funzioni cron per import di contenuti esterni non sono più utilizzate e sono state rimosse dalla codebase attiva.
